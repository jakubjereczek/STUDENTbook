import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.studenbook.pl',
  appName: 'studentBOOKmobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
